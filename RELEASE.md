# V2.4.0

- 新增两个数据源的定时健康检查；连续失败三次自动标为异常。
- 新增手机状态页以及 `/api/v1/status`、`/api/v1/matches`、`/api/v1/snapshot` 只读接口。
- 对外接口读取脱敏静态快照，不直接开放 DuckDB、原始响应或服务器配置。
- 实时和历史采集统一接入数据源适配器注册表，为经过测试的第三来源预留标准接入点。
- 新功能通过 systemd 自动启动和定时刷新，升级保留原数据库与所有回填断点。

## V2.3.0

- 按用户要求彻底停用中国体彩数据源，只保留 59itou 与 tiantianyouliao。
- 实时采集、历史回填、状态报告、质量检查和模型训练均只使用这两个来源。
- 既有数据库不清空；旧 `source_c` 记录保留为静态存档，但不再读取、显示或参与训练。
- 安装脚本增加强制代码覆盖和版本回显，避免虚拟环境继续调用旧版本。

## V2.2.0

- 修复中国体彩旧历史赛果接口返回 403 的问题：历史回填改为访问公开“足球赛果开奖”页面。
- 优先解析页面正常加载的官方 JSON；接口不可用时解析页面可见赛果表格。
- 继续复用原 DuckDB、原始对象和历史断点，不清空、不重抓已入库数据。
- 页面或日期段确实不可读取时明确报错，不跨过断点，不伪报回填成功。

## V2.1.0

- 增加中国体育彩票竞彩网公开实时赔率与官方赛果数据源。
- 官方历史赛果按七天窗口、每日最多八段低频回填，断点持续保存，目标范围至 2022-01-01。
- 官方接口遇到 WAF 或 HTTP 错误立即停止，不进行绕过或密集重试；其他两个数据源不受影响。
- 59itou、tiantianyouliao 页面未显示独立历史按钮时保留首页公开数据，不再让整次任务报错。

## V2.0.0

- 只使用 59itou 和 tiantianyouliao 两个配置来源。
- 新增历史入口自动导航、加载更多/上一页/更早/滚动分页。
- 优先识别接口页码并直接从下一页续传，无法识别时使用浏览器页码重放。
- 回填状态、最早/最晚比赛日期、连续无新增次数持久化到 DuckDB。
- 原始 JSON 改为 SHA-256 内容寻址和 gzip 压缩，相同内容全局只存一份。
- 赔率只记录变化，赛果、比赛、客观模型字段均幂等去重。
- 数据库迁移保留已有记录，不重新导入既有探针数据。
- 增加定时实时采集、历史回填、训练回测、校验备份和无损维护。
- 专家、推荐、会员、购买、支付和下单相关数据不进入分析库。
